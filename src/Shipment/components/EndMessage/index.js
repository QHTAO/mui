import React from "react";
import { Box } from "@mui/material";
function EndMessage() {
  return (
    <Box
      sx={{
        textAlign: "center",
        p: 1,
      }}
    ></Box>
  );
}

export default EndMessage;
